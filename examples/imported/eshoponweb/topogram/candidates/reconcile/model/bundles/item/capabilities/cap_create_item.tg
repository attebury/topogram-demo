# imported confidence: high
# imported source_kind: route_code
# imported provenance: src/Web/Controllers/ManageController.cs#POST /
# imported provenance: src/Web/Controllers/UserController.cs#POST /
capability cap_create_item {
  name "POST /"
  description "Candidate capability imported from brownfield API evidence"

  creates [entity_item]

  status active
}
