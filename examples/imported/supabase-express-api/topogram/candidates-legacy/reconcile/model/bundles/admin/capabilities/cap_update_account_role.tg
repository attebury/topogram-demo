# imported confidence: medium
# imported source_kind: route_code
# imported provenance: src/routes/admin.ts#PUT /admin/accounts/:id/role
capability cap_update_account_role {
  name "PUT /admin/accounts/:id/role"
  description "Candidate capability imported from brownfield API evidence"

  updates [entity_admin]

  status active
}
