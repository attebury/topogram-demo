# imported confidence: medium
# imported source_kind: route_code
# imported provenance: src/routes/admin.ts#GET /admin/memberships
capability cap_list_all_memberships {
  name "GET /admin/memberships"
  description "Candidate capability imported from brownfield API evidence"

  reads [entity_admin]

  status active
}
