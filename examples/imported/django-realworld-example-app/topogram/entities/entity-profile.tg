# imported confidence: high
# imported source_kind: schema
# imported provenance: conduit/apps/profiles/models.py
entity entity_profile {
  name "Profile"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    user user required
    bio string optional
    image string optional
    follows profile required
    favorites article required
  }

  status active
}
