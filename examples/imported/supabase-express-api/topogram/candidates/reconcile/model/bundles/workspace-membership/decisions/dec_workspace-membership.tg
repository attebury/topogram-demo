# imported confidence: medium
# imported source_kind: generated_artifact
# imported provenance: src/docs/openapi.ts#PUT /workspaces/{id}/members/{memberId}/role
decision dec_workspace-membership {
  name "Workspace Membership Workflow"
  description "Candidate workflow decision imported from brownfield evidence"

  context [cap_create_workspace_membership, cap_delete_workspace_membership]
  consequences [created, deleted]

  status proposed
}
