# imported confidence: high
# imported source_kind: route_code
# imported provenance: config/routes.rb#GET /api/tags
# imported provenance: app/controllers/tags_controller.rb
capability cap_list_tags {
  name "GET /api/tags"
  description "Candidate capability imported from brownfield API evidence"

  actors [user]

  reads [entity_tag]
  output [shape_output_list_tags]

  status active
}
