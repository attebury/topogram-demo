# imported confidence: high
# imported source_kind: schema
# imported provenance: CountriesSwiftUI/Repositories/Models/CountryDetails.swift
entity entity_country_detail {
  name "Country Detail"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    capital string required
    currencies currency required
    neighbors country optional
  }

  keys {
    unique [capital]
  }

  status active
}
