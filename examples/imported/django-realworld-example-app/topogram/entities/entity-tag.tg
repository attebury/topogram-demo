# imported confidence: high
# imported source_kind: schema
# imported provenance: conduit/apps/articles/models.py
entity entity_tag {
  name "Tag"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    tag string required
    slug string required
  }

  keys {
    unique [slug]
  }

  status active
}
