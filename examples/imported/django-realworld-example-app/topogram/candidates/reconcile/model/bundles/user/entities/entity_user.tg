# imported confidence: high
# imported source_kind: schema
# imported provenance: conduit/apps/authentication/models.py
entity entity_user {
  name "User"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    username string required
    email string required
    is_active boolean required
    is_staff boolean required
  }

  keys {
    unique [username, email]
  }

  status active
}
