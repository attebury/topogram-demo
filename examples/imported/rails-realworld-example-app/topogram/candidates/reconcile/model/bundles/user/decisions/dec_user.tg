# imported confidence: medium
# imported source_kind: generated_artifact
# imported provenance: config/routes.rb#GET /api/users/:id
# imported provenance: config/routes.rb#GET /api/user
# imported provenance: app/controllers/users_controller.rb
decision dec_user {
  name "User Workflow"
  description "Candidate workflow decision imported from brownfield evidence"

  context [cap_create_user]
  consequences [registered]

  status proposed
}
