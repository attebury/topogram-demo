# imported confidence: high
# imported source_kind: route_code
# imported provenance: src/Web/Controllers/OrderController.cs#GET /{orderId}
capability cap_get_item {
  name "GET /{orderId}"
  description "Candidate capability imported from brownfield API evidence"

  reads [entity_item]

  status active
}
