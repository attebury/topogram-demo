# imported confidence: high
# imported source_kind: route_code
# imported provenance: config/routes.rb#GET /api/articles/:slug
# imported provenance: app/controllers/articles_controller.rb
capability cap_get_article {
  name "GET /api/articles/:slug"
  description "Candidate capability imported from brownfield API evidence"

  reads [entity_article]

  input [shape_input_get_article]
  output [shape_output_get_article]

  status active
}
